# Rubik3x3Solver

## 🚀 How to Run

To set up and run this project, follow these steps:

```bash
cd cuber
npm install
npm audit fix           # Optional: fix vulnerabilities
npm audit fix --force   # Optional: force fix if needed
npm run dev
```
The application will be available at: http://localhost:5173/
